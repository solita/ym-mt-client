import reducer from './reducers';

import * as eventOperations from './operations';
import * as eventSelectors from './selectors';

export { eventOperations, eventSelectors };

export default reducer;
